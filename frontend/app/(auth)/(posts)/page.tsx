export default function Posts() {
	// TODO: MAKE A REDIRECT TO LOGIN LATER IF USER IS NOT LOGGED IN
	// const cookieStore = cookies();

	// if (!(cookieStore.get('session')?.value && !CONFIG_DEV)) {
	//   redirect('/posts');
	// }
	return <div>Posts page</div>;
}
